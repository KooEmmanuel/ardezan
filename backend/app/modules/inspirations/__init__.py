"""Design inspirations — curated Bespoke showcase tiles.

Each inspiration is a starting point for the Design Me flow: a fabric +
piece + tested brief paired with a hero image. They power both the
``/catalog?cat=bespoke`` showcase grid and the inspiration row on
``/try-on/design``.

Admin-managed via ``/api/v1/admin/inspirations``; public read via
``GET /api/v1/inspirations``.
"""
